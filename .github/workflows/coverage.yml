name: Coverage

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

permissions:
  contents: read

env:
  COVERAGE_PREFIX: coverage/jia-parse

jobs:
  coverage:
    name: Generate and upload coverage
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable
        with:
          components: llvm-tools-preview

      - name: Install cargo-llvm-cov
        uses: taiki-e/install-action@cargo-llvm-cov

      - name: Generate HTML coverage
        run: cargo llvm-cov --workspace --html --output-dir target/llvm-cov/html

      - name: Generate LCOV coverage
        run: cargo llvm-cov --workspace --lcov --output-path target/llvm-cov/lcov.info

      - name: Generate coverage summary
        run: cargo llvm-cov --workspace --summary-only | tee target/llvm-cov/summary.txt

      - name: Prepare upload
        run: |
          cp target/llvm-cov/lcov.info target/llvm-cov/html/lcov.info
          cp target/llvm-cov/summary.txt target/llvm-cov/html/summary.txt

      - name: Upload coverage to S3
        env:
          AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
          AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          AWS_REGION: ${{ secrets.AWS_REGION }}
          AWS_ENDPOINT_URL_S3: ${{ secrets.AWS_ENDPOINT_URL_S3 }}
          BUCKET_NAME: ${{ secrets.BUCKET_NAME }}
        run: |
          aws s3 sync target/llvm-cov/html "s3://${BUCKET_NAME}/${COVERAGE_PREFIX}/" \
            --endpoint-url "${AWS_ENDPOINT_URL_S3}" \
            --delete
